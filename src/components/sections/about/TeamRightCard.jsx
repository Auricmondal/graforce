import CardWrapper from '@/wrappers/CardWrapper'
import React from 'react'

const TeamRightCard = ({ className }) => {
  return (
    <CardWrapper color='transparent' className={`${className}`}>
      TeamRightCard
    </CardWrapper>
  )
}

export default TeamRightCard